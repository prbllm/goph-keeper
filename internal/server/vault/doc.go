// Package vault implements the server-side vault engine: CRUD with optimistic concurrency,
// tombstone deletes, revision log entries, and idempotent sync operations via processed_operations.
package vault
