// Package main — точка входа для CLI-клиента "goph-keeper"
// Предоставляет интерфейс командной строки для управления секретами,
// аутентификации и синхронизации с сервером.
package main

import (
	"os"

	"github.com/prbllm/goph-keeper/internal/cli"
)

// main — основная точка входа приложения.
// Выполняет запуск CLI-интерфейса и обрабатывает ошибки выполнения.
// Возвращает код выхода 1 в случае возникновения ошибки.
func main() {
	if err := cli.Execute(); err != nil {
		os.Exit(1)
	}
}
